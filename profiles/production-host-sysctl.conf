# TASK-73.08: dual-Spark FP8 production host prerequisite (orcus and warda).
# The packed PLE mmap and CUDA allocations do not use static HugeTLB pages.
# An unused 4096 x 2 MiB reservation removed 8 GiB from orcus host headroom.
# Verify HugePages_Free == HugePages_Total and HugePages_Rsvd == 0 before
# releasing an existing pool; do not reclaim pages used by another workload.
vm.nr_hugepages = 0
