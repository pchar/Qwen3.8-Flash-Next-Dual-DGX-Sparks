# Tune 9 — C1: GMU 0.775 (convergence sweep, bisection step 1 of the 0.75/0.80 boundary)

- **Ticket:** TASK-73.09
- **Date:** 2026-09-22
- **Branch/commit:** m8-tune @ <profile commit SHA — fill at record time> (profile) + this record
- **Profile:** profiles/c1-gmu0.775.env
- **Image digest:** sha256:d464f3b466fa9c45ddbff8a812e80564503b6879a9fd95c1a47514f3f0df5a4a
- **Model revision:** Qwen/Qwen3.8-Flash-Next-FP8 (HF cache pin 236dfdf285828023ca3bcd3f37366c58a3469b13, HF_HUB_OFFLINE=1)
- **Protocol:** full standard restart (docker stop vllm-fn both nodes -> drop_caches both -> relaunch ./start-fp8.sh --no-download)
- **Single knob vs LKG (GMU 0.75 / MTP0 / MNT 8192 / seqs 8 / mamba bf16 / EP false):** GPU_MEMORY_UTILIZATION 0.75 -> 0.775

## Decision block (recorded BEFORE test)

- Previous result: LKG GMU 0.75 PASS (002/005/008, eval 89/100); GMU 0.80 FAIL (003, memwatch kill at warmup).
- Rule: PHASE A bisection — midpoint of interval (safe, unsafe) = (0.75, 0.80) -> (0.75+0.80)/2 = 0.775.
- Search state pre-test: interval (0.75, 0.80). Update on result: PASS -> safe := 0.775, next interval (0.775, 0.80); FAIL -> unsafe := 0.775, next interval (0.75, 0.775).
- Config to test: GMU 0.775, everything else = LKG.

## Config

| knob | value |
|------|-------|
| GPU_MEMORY_UTILIZATION | **0.775** (was 0.75) |
| KV_CACHE_DTYPE | fp8 |
| MAX_NUM_BATCHED_TOKENS | 8192 |
| MTP_NUM_SPECULATIVE_TOKENS | 0 |
| ENABLE_EXPERT_PARALLEL | false |
| PLE_OFFLOAD | true (packed FP8 table) |
| MAMBA_SSM_CACHE_DTYPE | bfloat16 |
| TENSOR_PARALLEL_SIZE | 2 (nnodes 2) |
| MAX_MODEL_LEN / MAX_NUM_SEQS | 262144 / 8 |

## Launch

- exact launch command: <fill — .last_head_launch.sh at launch>
- launch time: <fill>

## Boot — <PENDING>

<fill: health 200 poll time, weights 131/131, PLE registrations both nodes, OOM/Xid/watchdog/orphan scan>

## Capacity — <PENDING>

<fill: GPU KV cache size tokens, 262K concurrency, MemAvailable both nodes>

## Perf — <PENDING>

bench/decodebench.py --decode 400 --contexts 1000,200000 --temps 0.0 --model qwen3.8-flash-next-fp8 (log: orcus:/tmp/decodebench-c1-gmu0.775.log)

| context | content | ptok | TTFT s | decode tok/s | LKG (0.75) tok/s |
|---|---|---|---|---|---|
| pending | | | | | |

## Standard eval (jupiter, full defaults) — <PENDING>

command: ~/.local/bin/tool-eval-bench --base-url http://orcus.lan:8888/v1

<fill: score, pass/partial/fail, report path>

## Verdict — <PENDING>

<fill: PASS/FAIL, bisection state update, LKG decision, rollback if FAIL>
