# Tune C10 — MNT 4096 at GMU* (PHASE B knob 2 alt) — in flight

- **Ticket:** TASK-73.18 (slot C10, PHASE B knob 2 alt: MAX_NUM_BATCHED_TOKENS 8192->4096)
- **Date:** 2026-09-25
- **Branch/commit:** m8-tune (profile f646458; result commit appended)
- **Profile:** `profiles/c10-mnt4096.env`
- **Image digest:** `sha256:d464f3b466fa9c45ddbff8a812e80564503b6879a9fd95c1a47514f3f0df5a4a`
- **Model revision:** Qwen/Qwen3.8-Flash-Next-FP8 (HF cache pin `236dfdf285828023ca3bcd3f37366c58a3469b13`)
- **Protocol:** full standard restart; eval after boot; perf after eval (sequential)

## Decision (recorded BEFORE deploy)

- Knobs resolved so far: MTP -> 0 (C8 crash), MAMBA -> bf16 (C12 invalid), EP excluded (006).
- MNT: 16384 FAIL-crash (C9), 8192 survives (C7), 4096 FAILed eval 83 at GMU 0.75 (004).
- **C10 = MNT 8192 -> 4096 at GMU*** — the untested third point of the MNT dicotomy.
  Question: does a lower batch budget (smaller prefill/decode chunks) buy anything at
  GMU* beyond what 8192 already gives, or does it only lose eval (004 precedent)?
  Keep MNT 4096 iff eval >= 86 AND survives decodebench; else MNT stays 8192.

## Config (single knob vs GMU* C7)

| knob | value |
|------|-------|
| GPU_MEMORY_UTILIZATION | 0.799609375 (unchanged = GMU*) |
| MAX_NUM_BATCHED_TOKENS | **8192 -> 4096** |
| MAX_NUM_SEQS | 8 (unchanged) |
| MTP_NUM_SPECULATIVE_TOKENS | 0 |
| MAMBA_SSM_CACHE_DTYPE | bfloat16 |
| KV_CACHE_DTYPE | fp8 |
| ENABLE_EXPERT_PARALLEL | false |
| TENSOR_PARALLEL_SIZE | 2 (nnodes 2) |
| MAX_MODEL_LEN | 262144 |

## Boot

- **PASS (after 1 fix-retry of the environment, 0 fix-retries of the config).**
- Launch command (head orcus): `cd /home/pch/MyProjects/Qwen3.8-Flash-Next-Dual-DGX-Sparks && ./start-fp8.sh --no-download` (launches both ranks; head side drives worker over ssh).
- **Attempt 1 (10:37Z): boot killed by the 6 GiB host-memwatch floor** — MemAvailable
  collapsed 36.4 GiB -> 6.1 GiB in ~30 s right after KV sizing (during FlashInfer JIT
  autotune), `docker kill vllm-fn` at 10:48:06Z (memwatch-head.log, local 12:48 CEST). KV had sized to
  4,704,954 (17.95x) but never served. No config fault: same knife-edge host-RAM
  pressure as observed across C5-C9; warda rank-1 orphan remained (100 GB GPU).
- **Attempt 2 (10:49Z): rejected** — `REQUIRE_IDLE_GPU` guard: worker still held the
  orphaned rank (VLLM::Worker_TP1, 100434 MiB).
- Cleanup: `docker stop vllm-fn` warda, `sudo sync && drop_caches` both nodes.
- **Attempt 3 (10:55Z): PASS** — shards 131/131, KV sized 4,074,596 (15.54x @262K,
  Available KV 33.56 GiB; lower than attempt 1's 4.7M because of residual host-RAM
  state at KV-sizing time — allocator-dependent, same knife edge). Health 200 ~14 min
  after launch (11:12Z, boot started ~10:58Z). Engine config confirmed: `max_num_batched_tokens=4096`,
  `speculative_config=None` (MTP0), `max_seq_len=262144`, `tensor_parallel_size=2`,
  `kv_cache_dtype=fp8`. PLE packed-table registrations both nodes (head 4 layer refs,
  worker 1). /v1/models: qwen3.8-flash-next-fp8, max_model_len 262144.
- MemAvailable post-boot: orcus ~7.8 GiB (floor 6 intact, margin ~1.8 GiB — tightest
  post-boot margin of the sweep, vs C7 ~8.7).
- 0 OOMKilled, 0 Xid, no orphan rank at end of boot; container up, watchdog active
  (floor 6 GiB, never lowered).

## Eval (standard)

- IN FLIGHT — `tool-eval-bench --base-url http://orcus.lan:8888/v1` (full defaults)
  launched 2026-09-25 11:13Z from jupiter.lan, log `~/tune/c10-eval.log`.

## Perf leg (decodebench)

- PENDING (after eval, sequential): `python3 bench/decodebench.py --decode 400
  --contexts 1000,200000 --temps 0.0 --model qwen3.8-flash-next-fp8` on orcus.

## Verdict

- PENDING — keep MNT 4096 iff eval >= 86 AND survives decodebench; else MNT stays 8192.
