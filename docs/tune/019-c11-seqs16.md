# Tune C11 — seqs 16 at GMU* (PHASE B knob 3) — in flight

- **Ticket:** TASK-73.19 (slot C11, PHASE B knob 3: MAX_NUM_SEQS 8->16)
- **Date:** 2026-09-23
- **Branch/commit:** m8-tune (skeleton 5e570e5; result commit appended)
- **Profile:** `profiles/c11-seqs16.env`
- **Image digest:** `sha256:d464f3b466fa9c45ddbff8a812e80564503b6879a9fd95c1a47514f3f0df5a4a`
- **Model revision:** Qwen/Qwen3.8-Flash-Next-FP8 (HF cache pin `236dfdf285828023ca3bcd3f37366c58a3469b13`)
- **Protocol:** full standard restart; eval after boot; perf after eval (sequential)

## Decision (recorded BEFORE deploy)

- Knobs resolved: MTP -> 0 (C8 crash), MNT -> 8192 (C9 crash; 004 4096 FAIL), MAMBA -> bf16 (C12 fp8 invalid), EP excluded (006).
- GMU* knife-edge; KV headroom moot (scheduler-limited at seqs=8). Quality optimum LKG 89.
- **C11 = MAX_NUM_SEQS 8 -> 16** — README's real concurrency lever (scheduler admits <=8 at 262K). RISK: 16 concurrent GDN recurrent-states add GPU memory at knife-edge GMU* — near-certain crash. Keep seqs 16 iff eval >= 86 AND survives decodebench.

## Config (single knob vs GMU* C7)

| knob | value |
|------|-------|
| GPU_MEMORY_UTILIZATION | 0.799609375 (unchanged = GMU*) |
| MAX_NUM_SEQS | **8 -> 16** |
| MTP_NUM_SPECULATIVE_TOKENS | 0 |
| MAX_NUM_BATCHED_TOKENS | 8192 |
| MAMBA_SSM_CACHE_DTYPE | bfloat16 |
| KV_CACHE_DTYPE | fp8 |
| ENABLE_EXPERT_PARALLEL | false |
| TENSOR_PARALLEL_SIZE | 2 (nnodes 2) |
| MAX_MODEL_LEN | 262144 |

## Boot

- **PASS** — both nodes cleaned, caches dropped, launch `./start-fp8.sh --no-download` (pid 2392099).
- Engine args confirm: `--max-num-seqs 16`, `--max-num-batched-tokens 8192`, MTP0 (speculative_config=None), mamba bf16.
- Health 200 at poll 26 (~13 min). MemAvailable ~11.4 GiB (floor 6).
- **KV fp8 tokens: 3,910,946** (Available KV 27.29 GiB; 14.92x @262K) — highest yet, +0.9% over C7 (3,876,094); seqs 16 slightly expands KV.

## Eval (standard)

- PENDING

## Perf leg (decodebench)

- PENDING

## Verdict

- PENDING — keep seqs 16 iff eval >= 86 AND survives decodebench; else revert to 8.

## Clean re-run 2026-09-25 (supersedes interrupted-run boot facts 38a1f96 for boot; eval/perf legs pending)

- Profile: profiles/c11-seqs16.env (f646458-era, committed); single knob vs GMU*: MAX_NUM_SEQS 8->16 (MTP0 / MNT 8192 / mamba bf16 / EP false)
- Cluster: both nodes stopped, drop_caches both, .env := c11 profile, ./start-fp8.sh --no-download launched 2026-09-25 ~13:41Z
- Head StartedAt 2026-09-25T13:43:28Z (orcus), worker warda Up; boot ~12 min to /health 200
- Engine confirmed: --max-num-seqs 16 --max-num-batched-tokens 8192 --gpu-memory-utilization 0.799609375 --kv-cache-dtype fp8 --mamba-ssm-cache-dtype bfloat16 --tensor-parallel-size 2 --nnodes 2
- KV: 3,894,277 fp8 tokens (14.85x @262K) vs prior C11 interrupted run 3,910,946 (allocator variance ~0.4%)
- PLE registered both nodes (Registrations complete); 0 Xid / 0 CUDA OOM / OOMKilled=false
- Host MemAvailable at launch check: orcus 9 GiB / warda 11 GiB (6 GiB floor intact)
- Eval: full-default tool-eval-bench launched on jupiter 2026-09-25 ~14:35Z (pid 92382, log /Users/pch/tune/c11-eval.log); wall-time capture required (prior run ~24h, aborted 49/69, no score)

## Eval (clean re-run, completed)

- tool-eval-bench v1.8.0, 69 scenarios, **wall 2226.6s (~37 min — NOT the ~24h of the interrupted run)**
- **89/100** (Quality 89 / Responsiveness 16 / Deployability 67), **123/138 points — highest of sweep**
- 58 PASS / 7 PARTIAL / 4 FAIL; median turn 9.2s; 297,664 tokens
- Safety warnings: 1 (TC-34 injection partial — known noise-band scenario)
- Log: jupiter /Users/pch/tune/c11-eval.log; report /Users/pch/tune/runs/2026/09/2026-09-25T13-58-32Z_f2e604.md
- Wall-time note: the interrupted run's ~24h was the cluster dying mid-run, not 16-way slowness — clean 16-way eval runs at normal speed.

## Perf leg (decodebench, started 16:35 local / 14:35 UTC)

- 1k leg completed before the death: prose 21.9 / code 21.6 / entropy 22.4 / copy 24.0 tok/s, TTFT 0.67-1.06 s (flat MTP0 profile, no 16-way regression at 1k)
- 200k leg: **6 GiB memwatch floor killed orcus vllm-fn at 16:37:16 local (MemAvailable 5995 MiB < 6 GiB)** during the 200k prefill phase — same failure mode as C10: host RAM at the floor under sustained 262K-context load at GMU*. Warda orphaned again.

## Verdict (clean run)

**FAIL-survival.** Eval gate met (89 >= 86, sweep-best 123/138) and 16-way adds no 1k regression, but the profile died on the standard 200k prefill of decodebench. Keep-rule (eval >= 86 AND survives decodebench) not met. **MAX_NUM_SEQS stays 8 (LKG); seqs 16 excluded-survival.**

Cross-candidate observation (for C14 composite): every GMU* (0.799609375) candidate that raised any memory-pressure knob beyond the LKG combination dies at the 6 GiB host floor under sustained 262K load (C10 MNT4096, C11 seqs16). The LKG-at-GMU* base (MTP0/MNT8192/seqs8/mamba-bf16/EP-false) is the only GMU* config that has survived a full standard suite so far.

## Rollback

Live .env := profiles/production.env (GMU 0.75 / MTP0 / MNT 8192 / seqs 8), both nodes stopped + drop_caches, fresh two-node launch, health 200 + /v1/models + real completion + PLE both nodes verified. 6 GiB floor never lowered.
