# Tune 12 — C4: GMU 0.796875 (convergence sweep, bisection step 4 of the 0.75/0.80 boundary)

- **Ticket:** TASK-73.12
- **Date:** 2026-09-23
- **Branch/commit:** m8-tune @ <this commit> (profile + record skeleton) + this record
- **Profile:** profiles/c4-gmu0.796875.env
- **Image digest:** sha256:d464f3b466fa9c45ddbff8a812e80564503b6879a9fd95c1a47514f3f0df5a4a
- **Model revision:** Qwen/Qwen3.8-Flash-Next-FP8 (HF cache pin 236dfdf285828023ca3bcd3f37366c58a3469b13, HF_HUB_OFFLINE=1)
- **Protocol:** full standard restart (docker stop vllm-fn both nodes -> drop_caches both -> relaunch ./start-fp8.sh --no-download)
- **Single knob vs LKG (GMU 0.75 / MTP0 / MNT 8192 / seqs 8 / mamba bf16 / EP false):** GPU_MEMORY_UTILIZATION 0.75 -> 0.796875

## Decision block (recorded BEFORE test)

- Previous result: C3 GMU 0.79375 PASS (stable boot, 0 OOM/Xid/watchdog, MemAvail 12.30/15 GiB post-boot; KV 3,760,932 = 14.35x; eval 85/100).
- Rule: PHASE A bisection — 0.79375 PASS -> safe := 0.79375; test midpoint of (0.79375, 0.80): (0.79375 + 0.80) / 2 = **0.796875** (already at 5-decimal precision).
- Search state pre-test: interval (0.79375, 0.80). Update on result: PASS -> safe := 0.796875, next interval (0.796875, 0.80); FAIL -> unsafe := 0.796875, next interval (0.79375, 0.796875).
- Config to test: GMU 0.796875, everything else = LKG.
- Risk note: 0.796875 is 0.003125 below the known-unsafe 0.80 and MemAvail margin is eroding (C1 16.96 / C2 13.17 / C3 12.30 GiB) — a memwatch kill at warmup is the most likely failure mode, as at 0.80 (003).

## Config

| knob | value |
|------|-------|
| GPU_MEMORY_UTILIZATION | **0.796875** (was 0.75) |
| KV_CACHE_DTYPE | fp8 |
| MAX_NUM_BATCHED_TOKENS | 8192 |
| MTP_NUM_SPECULATIVE_TOKENS | 0 |
| ENABLE_EXPERT_PARALLEL | false |
| PLE_OFFLOAD | true (packed FP8 table) |
| MAMBA_SSM_CACHE_DTYPE | bfloat16 |
| TENSOR_PARALLEL_SIZE | 2 (nnodes 2) |
| MAX_MODEL_LEN / MAX_NUM_SEQS | 262144 / 8 |

## Launch

- head launch: ./start-fp8.sh --no-download on orcus (launches both ranks; rendered .last_head_launch.sh contains --gpu-memory-utilization 0.796875)
- pre-launch: docker stop + docker rm vllm-fn on orcus AND warda; vm page cache evicted (sync + drop_caches) on both nodes
- .env on orcus: C3 body with GMU 0.79375 -> 0.796875 (live extras preserved: ABLIT=1, HF_TOKEN, WORKER_USER, YARN_FACTOR); backup at .env.bak-c4-20260923

## Boot

- PASS — orcus vllm-fn Up ~40 min, restarts 0; warda Up ~40 min, restarts 0; OOMKilled false both.
- orcus /health 200 (polled ~30 min to 200); /v1/models serves qwen3.8-flash-next-fp8 (max_len 262144); real completions confirmed (eval traffic, HTTP 200).
- 0 OOM / Xid / watchdog-kill / orphan-rank lines in vllm-fn logs on orcus or warda.
- PLE offload active both nodes (PleOffloadWorker rank 0/1 registered, packed FP8 table loaded).
- MemAvailable post-boot: orcus 12 GiB / warda 12 GiB (6 GiB floor intact).

## Capacity

- KV cache: 3,862,456 fp8 tokens, max concurrency at 262,144 ctx = **14.73x** (engine log 2026-09-23 01:37:54Z).
- vs C3 (GMU 0.79375): +101,524 tokens (14.35x -> 14.73x) for +0.003125 GMU — consistent with the ~+135k/0.00625 slope (C2->C3); no cliff yet.
- vs LKG (GMU 0.75): +1,904,624 tokens (11.28x -> 14.73x, +31%).

## Perf (run sequentially AFTER eval)

- Command (orcus): `python3 bench/decodebench.py --decode 400 --contexts 1000,200000 --temps 0.0 --model qwen3.8-flash-next-fp8`; log /tmp/decodebench-c4-gmu0.796875.log
- 1k context: prose 22.2 / code 22.2 / entropy 22.6 / copy 24.2 tok/s (TTFT 0.66-0.79s)
- 200k cold: prose TTFT **89.25s** (decode 22.0 tok/s); warm rows ~0.7-2.3s TTFT.
- Headline: **22.2 tok/s / 89.25s** — flat vs C1-C3 (21.9-22.2 / 86.99-88.72s); throughput unchanged by the GMU raise.

## Standard eval (jupiter, full defaults)

- Command: `~/.local/bin/tool-eval-bench --base-url http://orcus.lan:8888/v1`; log /tmp/m8-eval-c4-gmu0.796875.log; full report /Users/pch/runs/2026-09/2026-09-23T02-00-25Z_f2e604.md
- **Score: 86 / 100** (56 PASS / 7 PARTIAL / 6 FAIL of 69 TC).
- Categories at 100%: Tool Selection, Parameter Precision, Multi-Step Chains, Restraint & Refusal, Error Recovery, Localization, Structured Reasoning, Code Patterns. Soft: Safety & Boundaries 73%, Toolset Scale 75%.
- vs C3 (85) +1 point = inside the 85-89 run-to-run variance band; still below LKG 89 reference.

## Verdict

- **PASS-stable / no score gain** — healthy boot, 0 OOM/Xid/watchdog, KV 3,862,456 (14.73x), eval 86/100, perf flat.
- Bisection update: safe := 0.796875; next interval (0.796875, 0.80); GMU* = 0.796875 (new max stable GMU).
- Decision for next run (C5): midpoint of (0.796875, 0.80) = **0.7984375** (7-decimal); risk: MemAvail eroding (16.96 / 13.17 / 12.30 / 12 GiB) and only 0.0015625 below the known-unsafe 0.80 — memwatch kill at warmup remains the likely failure mode.
