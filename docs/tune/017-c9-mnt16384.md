# Tune C9 — MNT 16384 at GMU* (PHASE B knob 2) — in flight

- **Ticket:** TASK-73.17 (slot C9, PHASE B knob 2: MAX_NUM_BATCHED_TOKENS 8192->16384)
- **Date:** 2026-09-23
- **Branch/commit:** m8-tune (skeleton f3b7a49; result commit appended)
- **Profile:** `profiles/c9-mnt16384.env`
- **Image digest:** `sha256:d464f3b466fa9c45ddbff8a812e80564503b6879a9fd95c1a47514f3f0df5a4a`
- **Model revision:** Qwen/Qwen3.8-Flash-Next-FP8 (HF cache pin `236dfdf285828023ca3bcd3f37366c58a3469b13`)
- **Protocol:** full standard restart (stop both -> drop_caches both -> relaunch); eval after boot; perf after eval (sequential)

## Decision (recorded BEFORE deploy, in convergence.md)

- C8 FAIL: MTP3+47k at GMU* crashed (SIGKILL 137, GPU OOM NV_ERR_NO_MEMORY) under decode load after eval 85/100. MTP3+47k EXCLUDED-unsafe; MTP knob reverts to 0 (best-so-far).
- **C9 = MAX_NUM_BATCHED_TOKENS 8192->16384 at GMU*, MTP0** (MNT A/B vs 8192 best-so-far; 004 tested 4096@0.75 FAIL, 16384 untested). Keep MNT 16384 as best-so-far iff eval >= 86.

## Config (single knob vs GMU* MTP0)

| knob | value |
|------|-------|
| GPU_MEMORY_UTILIZATION | 0.799609375 (unchanged = GMU*) |
| MAX_NUM_BATCHED_TOKENS | **8192 -> 16384** |
| MTP_NUM_SPECULATIVE_TOKENS | 0 (reverted from C8's 3) |
| MTP_DRAFT_VOCAB | "" (unset) |
| KV_CACHE_DTYPE | fp8 |
| ENABLE_EXPERT_PARALLEL | false |
| TENSOR_PARALLEL_SIZE | 2 (nnodes 2) |
| MAX_MODEL_LEN / MAX_NUM_SEQS | 262144 / 8 |

## Boot — PASS (verified)

- Both nodes cleaned (C8 container removed), caches dropped, launch `./start-fp8.sh --no-download` (pid 3862398).
- Engine args confirm: `--gpu-memory-utilization 0.799609375`, `max_num_batched_tokens 16384`, MTP0 (speculative_config=None), chunked prefill 16384.
- Health 200 at poll 26 (~13 min). MemAvailable ~13.0 GiB (floor 6).
- **KV fp8 tokens: 3,347,260** (Available KV 23.27 GiB; 12.77x @262K) — MNT 16384 changes KV block layout, ~13% less than C7's 3,876,094 (14.79x).

## Eval (standard, tool-eval-bench v1.8.0)

- **Score: 86/100** (Quality 86 / Deployability 65 / Responsiveness 16; median turn 9.1s; 55 pass / 9 partial / 5 fail; weakest J Code Patterns 67%; safety 2: TC-34 partial, TC-58 leaked fake key)
- Run 2026-09-23T12-31-58Z_f2e604; 296,436 tokens; 2166.4s.

## Perf leg (decodebench, after eval)

- **CRASHED** — 1k contexts measured 22.1-24.3 tok/s (TTFT 0.66-1.12s, = MTP0 baseline), then engine SIGKILL 137 on the 200k prefill. dmesg: `NVRM: Check failed: Out of memory [NV_ERR_NO_MEMORY]` — MNT 16384's larger prefill chunk spikes peak activation over the GPU limit at GMU*.

## Verdict

- **FAIL** — MNT 16384 at GMU* boots + serves eval (86) but crashes under 200k decode load (GPU OOM NV_ERR_NO_MEMORY, SIGKILL 137), same failure class as C8. Per README/004, MNT 8192 is the interactive-vs-long-context winner (larger chunk = higher activation, no latency gain). **MNT knob resolves to 8192** (C6/C7, 86/100, survives decode load). MNT 16384 excluded-crash; MNT 4096 excluded from 004.
- **Reframe (from repo knowledge):** GMU* 0.799609375 is knife-edge — only MNT 8192 + MTP0 survives decode load; any memory-raising knob crashes. And KV headroom is moot: at MAX_NUM_SEQS=8 the scheduler admits fewer requests than the cache holds (README), so Phase A's KV gains don't lift serving. Quality optimum is LKG (89).
