# Tune 14 — C6: GMU 0.79921875 (convergence sweep, bisection step 6 of the 0.75/0.80 boundary)

- **Ticket:** TASK-73.14
- **Date:** 2026-09-23
- **Branch/commit:** m8-tune @ 547b3f9 (profile + record skeleton) + this record commit
- **Profile:** profiles/c6-gmu0.79921875.env
- **Image digest:** sha256:d464f3b466fa9c45ddbff8a812e80564503b6879a9fd95c1a47514f3f0df5a4a
- **Model revision:** Qwen/Qwen3.8-Flash-Next-FP8 (HF cache pin 236dfdf285828023ca3bcd3f37366c58a3469b13, HF_HUB_OFFLINE=1)
- **Protocol:** full standard restart (docker stop vllm-fn both nodes -> drop_caches both -> relaunch ./start-fp8.sh --no-download)
- **Single knob vs LKG (GMU 0.75 / MTP0 / MNT 8192 / seqs 8 / mamba bf16 / EP false):** GPU_MEMORY_UTILIZATION 0.75 -> 0.79921875

## Decision block (recorded BEFORE test)

- Previous result: C5 GMU 0.7984375 PASS (stable boot, 0 OOM/Xid/watchdog, MemAvail 11/14 GiB post-boot; KV 3,876,094 = 14.79x; eval 83/100).
- Rule: PHASE A bisection — 0.7984375 PASS -> safe := 0.7984375; test midpoint of (0.7984375, 0.80): (0.7984375 + 0.80) / 2 = **0.79921875** (8-decimal).
- Search state pre-test: interval (0.7984375, 0.80). Update on result: PASS -> safe := 0.79921875, next interval (0.79921875, 0.80); FAIL -> unsafe := 0.79921875, next interval (0.7984375, 0.79921875).
- Config to test: GMU 0.79921875, everything else = LKG.
- Risk note: 0.79921875 is only 0.00078125 below the known-unsafe 0.80 and MemAvail has eroded to ~10-11 GiB (C5 11) — a memwatch kill at warmup is the near-certain outcome, as at 0.80 (003). Expected KV gain over C5: ~7-9k tokens if it boots.

## Config

| knob | value |
|------|-------|
| GPU_MEMORY_UTILIZATION | **0.79921875** (was 0.75) |
| KV_CACHE_DTYPE | fp8 |
| MAX_NUM_BATCHED_TOKENS | 8192 |
| MTP_NUM_SPECULATIVE_TOKENS | 0 |
| ENABLE_EXPERT_PARALLEL | false |
| PLE_OFFLOAD | true (packed FP8 table) |
| MAMBA_SSM_CACHE_DTYPE | bfloat16 |
| TENSOR_PARALLEL_SIZE | 2 (nnodes 2) |
| MAX_MODEL_LEN / MAX_NUM_SEQS | 262144 / 8 |

## Launch

- head launch: `./start-fp8.sh --no-download` on orcus (launches both ranks); container cmdline verified: `--gpu-memory-utilization 0.79921875 --max-num-seqs 8 --max-num-batched-tokens 8192 --kv-cache-dtype fp8 --mamba-ssm-cache-dtype bfloat16`, MTP0 (no speculative-config), nnodes 2
- pre-launch: docker stop + docker rm vllm-fn on orcus AND warda; vm page cache evicted (sync + drop_caches) on both nodes
- .env on orcus: C5 body with GMU 0.7984375 -> 0.79921875 (live extras preserved); backup at .env.bak-c6-20260923

## Boot

- PASS — orcus vllm-fn StartedAt 2026-09-23T04:09:29Z, warda 04:09:14Z; restarts 0 both; OOMKilled false both.
- orcus /health 200 at 04:21:44Z (~12.4 min boot, same as C5); /v1/models serves qwen3.8-flash-next-fp8; real completion confirmed (eval + perf legs ran against it).
- 0 OOM / Xid / watchdog-kill / orphan-rank / memwatch lines in vllm-fn logs on orcus or warda (grep count 0 both, incl. post-eval/perf).
- PLE: "Registrations complete (dp_size=1, tp_size=2)" both nodes (orcus 04:19:39Z, warda 04:13:15Z).
- MemAvailable post-boot: orcus 9 GiB / warda 14 GiB (6 GiB floor intact). Post-eval/perf: orcus 7 GiB / warda 11 GiB. No memwatch trip — margin kept eroding (C5 11/14 -> C6 9/14 post-boot) but held.

## Capacity

- KV cache: 3,854,880 fp8 tokens, max concurrency at 262,144 ctx = **14.71x** (engine log 2026-09-23 04:20:42Z).
- vs C5 (GMU 0.7984375): **−21,214 tokens (14.79x -> 14.71x)** despite +0.00078125 GMU — non-monotonic KV allocation (block/rounding granularity at the allocator boundary); the marginal KV gain per GMU increment has hit ~0 before the 0.80 cliff.
- vs LKG (GMU 0.75): +1,897,048 tokens (11.28x -> 14.71x, +31%).

## Eval (full defaults, no extra flags)

- Command: `~/.local/bin/tool-eval-bench --base-url http://orcus.lan:8888/v1` (jupiter); log /tmp/eval-c6.log; full report /Users/pch/runs/2026/09/2026-09-23T04-24-30Z_f2e604.md
- **Score: 86/100** — 55 passed / 8 partial / 6 failed; quality 86, responsiveness 16, deployability 65; weakest M Autonomous Planning (50%); completed in 2335s.
- vs C5 (83): +3, inside the established 83-89 variance band; still below the LKG 89 reference.

## Perf (run sequentially AFTER eval)

- Command (orcus, repo dir): `python3 bench/decodebench.py --decode 400 --contexts 1000,200000 --temps 0.0 --model qwen3.8-flash-next-fp8`; log /tmp/decodebench-c6-gmu0.79921875.log
- 1k context: prose 24.1 / code 23.9 / entropy 24.0 / copy 24.4 tok/s (TTFT 0.59-0.62s)
- 200k: prose TTFT 0.59s, decode 22.9-23.1 tok/s — **CAVEAT: NOT a cold prefill.** Unlike C1-C5 (first 200k row = cold TTFT ~87-89s), the engine's one-time long-prefill warmup evidently occurred during the 39-min eval, so all 200k rows here are warm. Confirmed by a re-run (log /tmp/decodebench-c6-rerun.log) where 200k stayed warm (0.65-0.67s). The cold-200k figure is therefore not directly comparable to C1-C5; decode throughput is.
- Headline decode: **~24.0 tok/s (1k) / ~23.0 tok/s (200k)** — ~1.8 tok/s above C1-C5's ~22.0-22.4; likely warm-engine effect (long eval left the engine hot), same class as the cold-TTFT anomaly.

## Verdict

- **PASS-stable / no score gain** — healthy boot, 0 OOM/Xid/watchdog/memwatch (boot + eval + perf), MemAvail floor intact. safe := **0.79921875**; GMU* = 0.79921875; next interval (0.79921875, 0.80).
- KV stopped growing (−21k vs C5): the remaining headroom to 0.80 buys no measurable capacity. If C7 (0.799609375) FAILs, GMU* stays 0.79921875; if it PASSes, the marginal gain is ~0 tokens.
- Eval 86/100 remains inside the 83-89 band and below LKG 89 — GMU alone has not lifted the score; LKG (GMU 0.75, 89/100, 11.28x) remains the production reference.
